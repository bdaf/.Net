@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center"> 
	<h1 class="display-4">ContosoCraft</h1>
        @foreach(var product in Model.Products) {
            <h2>@product.Title</h2>
        }
</div>
